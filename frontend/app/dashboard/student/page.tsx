
'use client';

import Home from '../../page';

const StudentDashboard = () => {
  // Reuse the existing Home component as the student dashboard
  return <Home />;
};

export default StudentDashboard;
